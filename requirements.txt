# 核心依賴：Pi 本機模式、線上 Docker 模式、開發機測試都需要。
#
# 故意只寫最低版本、不鎖死確切版號：你 Mac 上是 Python 3.9，鎖死的版號很容易鎖到
# 剛好只支援 3.10+ 的新版（例如 Pillow 12.x 就要求 3.10+），pip 自己解依賴時會依照
# 你目前的 Python 版本自動挑「支援你這個版本的最新版」，比我手動鎖版號可靠。
flask>=3.0
pillow>=10.0
requests>=2.31
pyyaml>=6.0
